import { createAction } from 'redux-actions'

// Actions
export const setInitialValues = createAction('SET_INITIAL_VALUES')
export const updateCharacter = createAction('UPDATE_CHARACTER')
export const addBodyItem = createAction('ADD_BODY_ITEM')
