import { createAction } from 'redux-actions'

// Actions
export const setCharacterBlocks = createAction('SET_CHARACTER_BLOCKS')
export const updateCharacterBlocks = createAction('UPDATE_CHARACTER_BLOCKS')
export const addBodyItem = createAction('ADD_BODY_ITEM')
